# ESTADO.md — Bitácora del proyecto

> Este archivo es la memoria compartida entre sesiones de trabajo. Claude Code (VS Code) lo actualiza al final de cada sesión técnica. Cowork lo puede leer después —conectando la carpeta del repo vía el puente de dispositivo— para generar informes de avance o revisar qué se hizo, sin ejecutar ni instalar nada.
>
> Convención: entradas en orden cronológico inverso (la más reciente arriba). No borrar histórico salvo limpieza puntual acordada.

> **Nota de rama (`19.0`)**: esta rama es la variante de **instalación única**, creada desde `master` el 2026-08-17. Los scripts `01-prep-db.sh`/`02-odoo-setup.sh`/`03-setup-nginx.sh`/`04-enable-ssl.sh` (renombrados, sin sufijo `-multi`) ya se adaptaron aquí: rutas sin versión (`/opt/odoo`, `odoo.conf`, servicio `odoo`), rama detectada automáticamente (no se pregunta), `dbfilter = ^%d.*$` (prefijo), vhost `$DOMAIN *.$DOMAIN`, `pack-maker.sh` eliminado (es herramienta de desarrollo). Esto resuelve los pendientes #1-3 y #5 de la lista de abajo (que describen el trabajo tal como se planteó en `master`, antes de crear esta rama) — el resto de pendientes/histórico sigue siendo válido como contexto. **Ya probada en un servidor real de producción (17/08): ver sesión 2026-08-17 en el histórico.**

## Módulos en curso

_(No aplica — este repo no contiene módulos Odoo, solo scripts de infraestructura.)_

## Pendientes / próximos pasos

1. **Crear la variante de instalación única por versión** en ramas específicas del repo (por ahora solo existe `master`), a partir de los scripts `-multi` actuales. Decidir ahí mismo si `master_install.sh` necesita bifurcarse en dos variantes (multi vs. single).
   - **Requisito del usuario, importante**: en esta variante, quitar toda referencia a la versión de Odoo de rutas/nombres — `/opt/odoo` (sin `/18` ni `/19`), `odoo.conf` (no `odoo180.conf`), `odoo.log` (no `odoo180.log`), nombre de servicio `odoo` (no `odoo180`). Tiene sentido porque en una instancia única nunca convive más de una versión a la vez en la misma máquina (a diferencia de `-multi`, donde sí hace falta diferenciar). Esto afecta a `01-prep-db`, `02-odoo-setup`, `03-setup-nginx` y `04-enable-ssl` de la variante single: hay que revisar cada `$BRANCH_DOMAIN`/`$SERVICE_NAME`/`$BASE_INSTANCIA` usado como sufijo de ruta o nombre y quitarlo en esta variante.
2. **En el script de producción (single-instance), cambiar `dbfilter`** de `^%d$` (coincidencia exacta, válido solo para el caso multi-instancia por versión) a `^%d.*$` (prefijo), para que un dominio de cliente muestre todas sus bases (`maralva_real`, `maralva_pruebas`, `maralva_formacion`, `proasur_real`, etc.).
3. **Vhosts nginx de producción**: por cada dominio de instalación (`maralva.eu` con sus clientes subcontratados bajo `*.maralva.eu`, o el dominio propio de un cliente con su propio wildcard), dos bloques — `server_name <dominio>;` y `server_name *.<dominio>;` — cada uno con su propio certificado (apex/wildcard, ver Decisiones tomadas), para que los subdominios nuevos funcionen solo dando de alta el DNS, sin tocar nginx cada vez. No hace falta cambiar nada del `proxy_set_header` actual (ya reenvía `X-Forwarded-Host`, y `odoo.conf` ya tiene `proxy_mode = True`).
4. **Probar `scripts/04-enable-ssl-multi.sh` en un servidor real** una vez exista el certificado de `maralva18.maralva.eu` (o el dominio que se use), y confirmar que `nginx -t` pasa y el sitio sirve bien por HTTPS.
5. Cuando se creen los scripts de instalación única por versión (Pendientes #1-3), replicar el mismo patrón de `04-enable-ssl-multi.sh` (comprobar certificado → regenerar vhost con HTTPS) para esa variante.
6. **Instalar y autenticar `gh` (GitHub CLI) en el servidor** antes de correr `02-odoo-setup-multi.sh` para la rama `19.0` — `gh auth login` funciona sin entorno gráfico (da un código de un solo uso para autorizar desde cualquier navegador). Sin `gh`, el script sigue funcionando pero avisa y no crea los forks automáticamente.
7. **Probar en la VM clonada** el clonado resiliente de `02-odoo-setup-multi.sh` (fork automático + omitir repos sin rama `19.0` todavía) y revisar la lista de repos omitidos que imprima al final, contrastándola con `pack_maralva_base19.txt` para ver qué módulos quedarían sin disponibilidad.
8. ✅ El "Permission denied" resultó ser un espejismo: la VM se había instalado con Ubuntu 22.04 en vez de 24.04 por error de ISO (de ahí Python 3.10.12 y el fallo al compilar `gevent`). Se reinstaló con la ISO correcta y se relanzó `master_install.sh` **sin `sudo`** (como se recomendó) sin problemas de permisos — la hipótesis del `sudo` + llave SSH queda como buena práctica a mantener, aunque no llegó a confirmarse por separado en la VM descartada.
9. **Probar de extremo a extremo el flujo de actualización desde OCA** (`update_oca_upstream.sh` → `update_databases.sh` → `push_org_origin.sh` o `update_org_origin.sh`, recién creados/ampliados) en la VM de pruebas, una vez la instalación base termine bien.

✅ El certificado SSL (antes pendiente #5-6) quedó **resuelto e implementado, y generalizado por dominio** en `maralva-ops` — ver `../maralva-ops/ESTADO.md` y `../maralva-ops/certs/`.

✅ La integración `master_install.sh` ↔ certificado quedó **decidida**: `master_install.sh` NO invoca nada de esto — se queda tal cual (solo fases 1-3, HTTP). Certificar y activar HTTPS son pasos manuales y aparte, ejecutados después de comprobar que la instalación base funciona (ver Decisiones tomadas).

## Bugs conocidos

_(Ninguno abierto a día de hoy.)_

## Decisiones tomadas

- 2026-08-16 — Scripts `01-prep-db.sh`, `02-odoo-setup.sh`, `03-setup-nginx.sh` renombrados a `*-multi.sh` — motivo: son la variante **multi-instancia** (varias versiones de Odoo en un mismo servidor), pensada solo para desarrollo/pruebas; se deja sitio para una futura variante de instalación única por versión en ramas específicas, pensada para producción.
- 2026-08-16 — `pg_hba.conf` restringido a las redes de confianza de Maralva (`192.168.1.0/24` conexión directa, `192.168.100.0/24` VPN) con autenticación `scram-sha-256`, en vez de `trust` abierto a `0.0.0.0/0` — motivo: eliminaba por completo la contraseña para cualquier IP de internet. El uso real (pgAdmin remoto para mantenimiento) queda igual: mismo alcance de acceso, solo que ahora pide la contraseña del rol `odoo` (que se puede guardar en pgAdmin).
- 2026-08-16 — `admin_passwd` se deja **sin definir** en el `odoo.conf` generado, a propósito — decisión explícita del usuario: el máster password se gestiona desde el propio asistente "Crear base de datos" de Odoo, no desde este archivo. No "corregir" esto en el futuro sin volver a preguntar.
- 2026-08-16 — `list_db` se mantiene en su valor por defecto (`True`, no se añade `list_db = False`) — motivo: cada instancia aloja varias bases (Real, pruebas, formación) que necesitan poder listarse para tareas de mantenimiento.
- 2026-08-16 — `db_password` se mantiene como `odoo` por ahora (sin cambio), aunque con el fix de `pg_hba` ya sí se exige de verdad en conexiones remotas (antes, con `trust`, daba igual cuál fuera). Reforzarla queda como posible mejora futura, no decidida.
- 2026-08-16 — Para el filtrado multi-cliente en producción: el dominio raíz (`maralva.eu`) no necesita subdominio propio, porque Odoo ya sustituye `%d` en `dbfilter` por "lo que hay antes del primer punto del hostname", que para `maralva.eu` ya es `maralva`.
- 2026-08-16 — Para el certificado wildcard: se descarta migrar la zona DNS completa de `maralva.eu` fuera de SupremeDNS (por el riesgo sobre el correo del dominio, alojado en el mismo panel); se opta por delegar únicamente el nombre `_acme-challenge.maralva.eu` vía CNAME a DuckDNS. La limitación de DuckDNS que descartó su uso para la IP dinámica (el router del operador no lo soporta como cliente DDNS) **no aplica aquí**: el reto ACME se resuelve con una simple llamada `curl` desde el propio servidor, no depende del router.
- 2026-08-16 — Se piden **dos certificados Let's Encrypt separados** (`<dominio>` y `*.<dominio>`) en vez de uno combinado — motivo: la API de DuckDNS solo admite un valor TXT a la vez, y al compartir ambos el mismo nombre de reto (`_acme-challenge.<dominio>`), un certificado combinado necesitaría dos valores simultáneos que se pisarían entre sí. Implementado en `maralva-ops/certs/setup-ssl-duckdns.sh`.
- 2026-08-16 — Estos scripts (multi-instancia y, en el futuro, instalación única) no son solo para `maralva.eu`: se usarán también para clientes que subcontraten bajo el mismo dominio y para clientes con dominio propio en otro proveedor de DNS. Por eso `master_install.sh` ya pide `DOMAIN` como parámetro, y `certs/setup-ssl-duckdns.sh` se corrigió para pedir el dominio de igual forma en vez de tener `maralva.eu` hardcodeado (dato aportado por el usuario a mitad de sesión, tras una primera versión que sí lo tenía fijo).
- 2026-08-16 — `SOLDIGES/gdigital-custom` (nombre de org/repo antiguo) sustituido por `$ORGANIZACION/maralva-custom` en `02-odoo-setup-multi.sh` y `pack-maker.sh`, para que coincida con el remoto real (`MaralvaEU/maralva-custom`).
- 2026-08-16 — El flujo de instalación (`master_install.sh`) se queda **sin cambios**, sin invocar la certificación ni tocar HTTPS — motivo: certificar no siempre es necesario ni posible para toda instancia, y acoplar ambos repos (necesitar saber dónde está clonado `maralva-ops` en el servidor) añadía complejidad innecesaria. En vez de eso: (1) se instala y se comprueba que funciona por HTTP, (2) se certifica el dominio a mano con `maralva-ops/certs/setup-ssl-duckdns.sh` cuando proceda, (3) se activa HTTPS a mano con el nuevo `scripts/04-enable-ssl-multi.sh`, que primero comprueba que el certificado ya existe antes de tocar nginx.
- 2026-08-16 — Creado `scripts/04-enable-ssl-multi.sh`: regenera el vhost de una instancia `-multi` ya creada por `03-setup-nginx-multi.sh`, añadiendo `listen 443 ssl` (apex y wildcard, cada uno con su certificado) y una redirección 80→443. Aborta sin tocar nginx si no encuentra los certificados esperados en `/etc/letsencrypt/live/`.
- 2026-08-16 — `02-odoo-setup-multi.sh` ahora crea el fork automáticamente en `$ORGANIZACION` (OCB y cada repo de `reposoca.txt`) cuando no existe todavía, usando `gh repo fork ... --clone=false` — motivo: antes el script clonaba de OCA como fallback pero nunca creaba el fork, dejando el remote `origin` apuntando a un repo inexistente para cualquier `push` futuro. Se eligió `gh` CLI en vez de la API REST + token propio porque no requiere gestionar un secreto nuevo (basta con `gh auth login` una vez, que funciona sin entorno gráfico); si `gh` no está instalado/autenticado, el script avisa y sigue sin abortar.
- 2026-08-16 — Si un repo de `reposoca.txt` no tiene la rama pedida (ej. `19.0`) ni siquiera en OCA todavía, `02-odoo-setup-multi.sh` lo **omite con aviso** y sigue con el resto, en vez de abortar toda la instalación (comportamiento anterior, por `set -e`) — motivo: en una versión de Odoo recién salida es normal que varios repos de OCA aún no hayan migrado, y exigir el 100% bloquearía poder probar nada. Aclarado con el usuario que esto no cambia `addons_path` en `odoo.conf` (sigue apuntando a la carpeta `oca/` entera); el efecto real es que los módulos de `pack_maralva_base*.txt` que vivan en un repo omitido fallarán al instalar `maralva_base_internal` hasta que ese repo tenga la rama.
- 2026-08-16 — Aclarado (confusión del propio usuario, corregida en el momento): `pack_maralva_base18.txt`/`pack_maralva_base19.txt` son listas de **módulos de Odoo** a instalar (para generar el `depends` del manifest vía `pack-maker.sh`), no tienen relación con qué repos clona `02-odoo-setup-multi.sh` — eso lo decide únicamente `reposoca.txt`.
- 2026-08-16 — `01-prep-db-multi.sh` corregido para que `apt`/`apt-get` no se queden esperando confirmación interactiva de `needrestart` (hallazgo real durante la primera instalación en servidor): todas las llamadas van con `sudo env DEBIAN_FRONTEND=noninteractive NEEDRESTART_MODE=a`, y se añadió un `apt update && apt upgrade -y` inicial.
- 2026-08-16 — `scripts/setup_logrotate.sh` corregido a petición del usuario, revisándolo antes de que sea el siguiente paso a probar: el `postrotate` recargaba **nginx** en vez de avisar a Odoo de que reabriera su log (bug real — Odoo no soporta reabrir logs por señal, así que la rotación no habría servido de nada, el log activo se habría quedado siempre vacío). Sustituido por `copytruncate`, que no necesita avisar al proceso; se quitaron `create`/`su`/`postrotate`. **Corrección posterior (17/08, ver más abajo): `su` había que mantenerlo, no era solo cosa de `create`.**
- 2026-08-16 — `update_org_origin.sh` renombrado a `push_org_origin.sh` (sube local → tu fork), liberando el nombre `update_org_origin.sh` para un script nuevo con el sentido contrario (trae tu fork → local, rollback). Motivo: la pareja de nombres "update_X" (traer de X a local) queda simétrica y auto-explicativa: `update_oca_upstream.sh` trae de OCA, `update_org_origin.sh` trae de tu propio fork; solo `push_org_origin.sh` sube.
- 2026-08-16 — Diseñado y creado el flujo completo de actualización desde OCA en 4 scripts (antes solo había 2, sin conectar el paso de aplicar cambios a las bases de datos ni el de deshacer si algo sale mal): `update_oca_upstream.sh` (ampliado para generar `config/actualizaciones/modulos_<rama>_<fecha>.txt` con los módulos realmente cambiados, filtrando por presencia de `__manifest__.py`/`__openerp__.py` para no listar `.github`, `README`, etc. — verificado con una simulación en un repo de prueba), `update_databases.sh` (nuevo, aplica `-u <módulos>` a las BDs elegidas, parando/arrancando el servicio), `push_org_origin.sh` (con nueva pregunta de confirmación sobre si las BDs se actualizaron bien) y `update_org_origin.sh` (nuevo, rollback de código a `origin/$BRANCH` si las pruebas fallan). Pensado para lanzarse manualmente uno por uno en desarrollo o en una copia de producción, nunca en producción directamente.
- 2026-08-16 — Creado `.gitignore` para este repo (no tenía) excluyendo `config/actualizaciones/`, ya que son artefactos generados por servidor en cada ejecución de `update_oca_upstream.sh`, no contenido a compartir en el repo.
- 2026-08-16 — La VM de pruebas se había instalado por error con Ubuntu 22.04 en vez de 24.04 (ISO equivocada al crearla), lo que causaba un fallo real al compilar `gevent` (Python 3.10.12 de Jammy, frente al 3.12 de Noble que espera el `requirements.txt` de OCB). Se reinstaló con la ISO correcta; la reinstalación es rápida.
- 2026-08-17 — `scripts/setup_logrotate.sh`: se restauró la directiva `su odoo odoo`, que se había quitado por error al arreglar el bug del `postrotate`/`nginx`. `su` no es solo para `create` (que sigue sin hacer falta con `copytruncate`): logrotate se niega a rotar un log si su carpeta es escribible por un grupo que no sea `root` (aquí, `/var/log/odoo` es 770, grupo `odoo`) salvo que `su` le diga explícitamente qué usuario/grupo usar — confirmado con `sudo logrotate -d` en el servidor real, que mostraba "insecure permissions... Set su directive".
- 2026-08-17 — `update_oca_upstream.sh` marca con `[NUEVO]` al final de línea los módulos que no existían antes del `reset` (comprobado con `git cat-file -e <commit_anterior>:<módulo>`), para distinguirlos de los que ya existían y solo se actualizaron. `update_databases.sh` se ajustó (`awk '{print $1}'` tras el `cut`) para que esa anotación no se cuele en la lista de módulos que se pasa a `-u`. Verificado con una simulación en un repo de prueba (módulo nuevo + módulo modificado a la vez).
- 2026-08-17 — **Bug real encontrado en el primer despliegue de producción**: `scripts/04-enable-ssl.sh` (y su equivalente `04-enable-ssl-multi.sh` en `master`) comprobaba la existencia del certificado con `[ -f "$CERT_APEX/fullchain.pem" ]` sin `sudo` — como `/etc/letsencrypt/live/` es `root:root` modo `0700`, un usuario normal no puede ni atravesar ese directorio, así que el test siempre daba "no encontrado" aunque el certificado existiera de verdad (confirmado porque `setup-ssl-duckdns.sh`, que sí corre con `sudo`, había terminado con éxito momentos antes). Corregido a `sudo test -f ...` en las tres ramas (`19.0`, `18.0`, `master`) para que la comprobación funcione se invoque el script con `sudo` o sin él.

## Histórico de sesiones

### 2026-08-17 (rama `19.0`) — Primer despliegue real de producción

**Hecho:**
- **Despliegue real completo** de la rama `19.0` en una VM Ubuntu 24.04 virgen, con los dominios reales `maralva.eu` y `proasur.maralva.eu` (esta última cubierta por el wildcard, sin trabajo adicional). `master_install.sh` completado con éxito (recuperado de una desconexión SSH a mitad de `apt` con `dpkg --configure -a` + `apt --fix-broken install` antes de relanzar). Tres bases de datos creadas desde `IP:8069` (`proasurjnma`, `maralva`, `maralvaprs`); acceso pgAdmin confirmado; acceso HTTP por dominio confirmado con `dbfilter` filtrando correctamente cada base por prefijo. Snapshot de la VM tomado en este punto antes de tocar SSL.
- `wkhtmltopdf` sigue sin instalarse por script (decisión ya tomada, ver Pendientes) — el usuario lo instalará a mano por ahora.
- **Emitidos los certificados SSL reales** para `maralva.eu` (apex + wildcard) vía `maralva-ops/certs/setup-ssl-duckdns.sh`. Dos errores de configuración del propio usuario detectados y corregidos sobre la marcha (no son bugs de script): (1) `DUCKDNS_SUBDOMAIN` en `certs/.env` estaba puesto como `maralva.eu` (el dominio real) en vez del subdominio de la cuenta DuckDNS (`maralvaeu`) — causaba que la API de DuckDNS respondiera `KO` al fijar el TXT; (2) el registro `_acme-challenge.maralva.eu CNAME maralvaeu.duckdns.org` nunca se había creado en el panel de SupremeDNS — causaba `NXDOMAIN` persistente. Con ambos corregidos, los dos certificados se emitieron sin problemas.
- **Bug real de script encontrado y corregido**: `scripts/04-enable-ssl.sh` daba "No se encuentra certificado válido" pese a que los certificados sí existían — causa y fix documentados en Decisiones tomadas (comprobación de fichero sin `sudo` contra un directorio `root:root 0700`). Workaround inmediato usado en el servidor: relanzar con `sudo ./scripts/04-enable-ssl.sh` (al correr todo el script como root, el `[ -f ]` sin corregir también habría funcionado). Después de aplicar el fix, `04-enable-ssl.sh` quedó ejecutado con éxito en el servidor real (puertos `8069`/`8072`), `nginx -t` correcto, HTTPS activo.
- **Confirmado en el navegador**: tanto `https://maralva.eu` como `https://proasur.maralva.eu` redirigen a HTTPS y el `dbfilter` sigue filtrando las bases correctamente por dominio.
- Segunda snapshot de la VM tomada al cierre de esta sesión, con el despliegue base + SSL ya operativo.

**Pendiente para la próxima sesión:**
- Decidir e instalar `wkhtmltopdf` (pendiente histórico, ver nota en `master`/Decisiones — instalar a mano por ahora, revisar versión compatible con 18.0 y 19.0 antes de scriptarlo).
- Desplegar la rama `18.0` en la otra máquina (real, aún no iniciado).
- Implementar `maralva-ops/monitoring/check-services.sh` y `backup/cron-config.txt` (siguen vacíos).
- Extender el flujo de actualización desde OCA (`update_oca_upstream.sh`/`push_org_origin.sh`/`update_org_origin.sh`) para tratar `openupgradelib` igual que `02-odoo-setup.sh` (por ahora solo el clonado inicial lo trata como caso especial).

### 2026-08-16

**Hecho:**
- Renombrados `01-prep-db.sh` → `01-prep-db-multi.sh`, `02-odoo-setup.sh` → `02-odoo-setup-multi.sh`, `03-setup-nginx.sh` → `03-setup-nginx-multi.sh` (con `git mv`, historial preservado); `master_install.sh` actualizado; commiteado y pusheado a `origin/master`.
- `CLAUDE.md` y este `ESTADO.md` añadidos al control de versiones (commiteado y pusheado).
- Revisión de seguridad/bugs de los scripts `-multi`: `sudogrep` → `sudo grep`, `pg_hba.conf` restringido con contraseña a las dos redes reales, `unaccent` generalizado a todas las BDs existentes (antes solo `maralva18` hardcodeado), `SOLDIGES/gdigital-custom` → `$ORGANIZACION/maralva-custom`, validación de `ODOO_PORT`/`ODOO_CHAT_PORT` en `master_install.sh`. Commiteado y pusheado a `origin/master`.
- Diseño de arquitectura de producción cerrado para multi-cliente por subdominio (`dbfilter` de prefijo `^%d.*$`, vhosts nginx por dominio) — pendiente solo de implementar en los futuros scripts de instalación única (Pendientes #1-3).
- Cerrado y **completamente implementado** el certificado SSL: DuckDNS + CNAME delegado desde `_acme-challenge.<dominio>` (sin migrar la zona DNS ni tocar el correo), dos certificados Let's Encrypt separados por la limitación de un solo TXT en DuckDNS. Código en `maralva-ops/certs/` (`setup-ssl-duckdns.sh`, `duckdns-auth-hook.sh`, `duckdns-cleanup-hook.sh`, `.env.example`).
- Detectado y corregido a mitad de sesión que la primera versión del script de certificado tenía `maralva.eu` hardcodeado; se generalizó para pedir el dominio de forma interactiva, ya que estos scripts se reutilizarán para clientes subcontratados y con dominio propio, no solo para la marca propia.
- Aclarado el hueco que faltaba: ni `master_install.sh` invocaba la certificación, ni `03-setup-nginx-multi.sh` sabía generar HTTPS. Se decidió NO acoplar ambos repos automáticamente; en su lugar se creó `scripts/04-enable-ssl-multi.sh` como cuarto paso manual y aparte, que comprueba el certificado antes de regenerar el vhost con `443 ssl` + redirect. Probado generando el `nginx.conf` resultante con datos ficticios (sin `sudo`/nginx real) para verificar que el escapado de variables nginx (`$host`, `$request_uri`, etc.) sale literal y correcto.
- Antes de probar la instalación `-multi` en la VM clonada, revisado `02-odoo-setup-multi.sh` a petición del usuario: se detectó que el fallback a OCA nunca creaba el fork en `$ORGANIZACION` (dejando `origin` roto para futuros push), y que un solo repo sin la rama pedida abortaba toda la instalación por `set -e`. Corregidos ambos: fork automático vía `gh repo fork` (OCB incluido) y "omitir con aviso" en vez de abortar. Verificado con `bash -n` que el script sigue siendo sintácticamente correcto.
- Generada y publicada en GitHub la llave SSH de la VM clonada (Ubuntu Server 24.04); verificado con `ssh -T git@github.com`.
- **Hallazgo real de la primera prueba en servidor**: un Ubuntu Server recién instalado (mínimo, solo OpenSSH) no trae `git`, así que hay que instalarlo a mano (`sudo apt update && sudo apt install -y git`) antes de poder clonar `maralva-deploy` — `01-prep-db-multi.sh` instala `git`, pero vive dentro de este mismo repo, que necesita `git` para clonarse la primera vez. Documentado como prerrequisito manual en `CLAUDE.md`.
- **Segundo hallazgo real**: durante la instalación, `apt` se quedó esperando una confirmación interactiva de `needrestart` (qué servicios reiniciar tras actualizar librerías), bloqueando el script. Corregido en `01-prep-db-multi.sh`: todas las llamadas a `apt`/`apt-get` van ahora precedidas de `sudo env DEBIAN_FRONTEND=noninteractive NEEDRESTART_MODE=a`, y se añadió un `apt update && apt upgrade -y` inicial (buena práctica en servidor recién instalado, aunque no evita el problema de `needrestart` por sí solo).
- **Tercer hallazgo**: apareció un "Permission denied" en algún punto de la clonación/instalación de dependencias; resultó ser un efecto colateral de que la VM tenía Ubuntu 22.04 en vez de 24.04 (ISO equivocada) — al reinstalar con la ISO correcta y relanzar sin `sudo`, desapareció.
- Diagnosticado en vivo un fallo de compilación de `gevent` (Cython `CompileError`) al crear el venv: causado por Python 3.10.12 (Jammy) en vez de 3.12 (Noble); el `requirements.txt` de OCB ya trae pines de `gevent` distintos por versión de Python, así que con la ISO correcta debería resolverse solo.
- Revisado `scripts/setup_logrotate.sh` a petición del usuario (era el siguiente script a probar): corregido el bug del `postrotate` que recargaba nginx en vez de nada útil para Odoo, sustituyéndolo por `copytruncate`.
- Diseñado y creado el flujo completo de actualización desde OCA: ampliado `update_oca_upstream.sh` para generar el fichero de módulos cambiados; creados `update_databases.sh` y el nuevo `update_org_origin.sh` (rollback); renombrado el antiguo a `push_org_origin.sh`; añadida pregunta de confirmación de BDs en `push_org_origin.sh`. Añadido `.gitignore` al repo (no tenía) para excluir los ficheros de módulos generados por servidor.
- **Instalación `-multi` de la rama `18.0` completada con éxito** en la VM (Ubuntu 24.04, reinstalada): PostgreSQL, servicio `odoo180` y Nginx activos, `nginx -t` correcto, symlink de systemd creado.
- **Cuarto hallazgo, corregido**: el resumen final de `master_install.sh` mostraba `http://maralva18.loc` en vez de `http://maralva18.maralva.eu` — tenía el TLD `.loc` escrito literal (resto de una convención antigua de pruebas locales) en vez de usar `$DOMAIN`. Nginx sí quedó bien configurado (`maralva18.maralva.eu`); solo el texto informativo del script estaba mal. Corregido a `http://$MARCA$BRANCH_DOMAIN.$DOMAIN` en las dos líneas donde aparecía.
- **Incidencia local, resuelta**: el `git push` de `maralva-deploy` desde este checkout de Windows empezó a fallar con `Permission denied (publickey)`. Causa real: al añadir la clave del servidor nuevo a GitHub, el usuario había dejado solo esa (quitando la de este PC). Se volvió a añadir la clave de este equipo (`jnmaral68@gmail.com`) en github.com/settings/keys y el push volvió a funcionar. De paso se repobló el `known_hosts` de este Git Bash, que se había quedado sin la entrada de `github.com`.
- **Probada la instalación `-multi` de una segunda rama (`19.0`) en la misma VM que ya tenía la `18.0`** (puertos `9069`/`9072`, distintos de los de `18.0`) — el caso de uso principal de los scripts `-multi`.
- **Quinto hallazgo, real y ya resuelto**: al relanzar `master_install.sh` para `19.0` en la misma sesión SSH donde se había instalado `18.0` (que añade al usuario al grupo `odoo`), `git clone` falló con `Permission denied` al crear `/opt/odoo/19/...`, aunque los directorios y permisos generados por el script parecían correctos. Causa: la sesión SSH activa seguía sin tener el grupo `odoo` cargado (se añadió a mitad de esa misma sesión); el script comprueba la pertenencia con `id -nG "$REAL_USER"` (consulta la cuenta, no la sesión activa), así que no detecta el problema y no repite el `exec sg odoo`. Solución confirmada: cerrar sesión SSH y volver a entrar antes de relanzar el script. Documentado en `CLAUDE.md` como gotcha a tener en cuenta también en la futura instalación única.
- Añadido a `02-odoo-setup-multi.sh` el clonado especial de `openupgradelib` (OCA): no tiene ramas por versión de Odoo, solo `master`, así que no puede vivir en `reposoca.txt` (rompería con `--branch $BRANCH`). Para la instalación de `19.0` en curso, el usuario lo clonó a mano esa vez.
- **Corrección sobre `openupgradelib` (a raíz de una idea del usuario)**: la primera versión lo clonaba dentro de `oca/` de cada rama, como si fuera un módulo más — pero `openupgradelib` no tiene `__manifest__.py`, es una librería Python normal (no un addon), así que ahí no lo habría cargado nadie. Se corrigió a un **clon único y compartido** en `/opt/odoo/openupgradelib` (no depende de la versión, tiene sentido no duplicarlo por rama), con cada venv instalándolo en modo editable (`pip install -e`) durante la sección de dependencias Python. El usuario sigue sin clonarlo todavía en ninguna instancia real; queda listo para la próxima vez que se ejecute `02-odoo-setup-multi.sh`.
- **Probado `setup_logrotate.sh` en el servidor real**: se detectó que `sudo logrotate -d` se saltaba los logs de Odoo por "insecure permissions" — se había quitado `su odoo odoo` por error al arreglar el bug del `postrotate` (esa directiva no era solo para `create`). Restaurada y **confirmado en el servidor** que `logrotate -d` ya no salta el aviso.
- **Probado `update_oca_upstream.sh` en el servidor real** (rama `18.0`): terminó bien, la mayoría de repos sin cambios (esperable en una rama madura), generó correctamente el fichero de módulos y el mensaje de siguiente paso. Se confirmó que las divergencias (`AHEAD > 0`) solo se registran en el log, no bloquean el reset a upstream — el usuario confirmó que ese comportamiento le vale tal cual.
- A petición del usuario, añadida la marca `[NUEVO]` para distinguir módulos que no existían antes del reset (nuevos en OCA) de los que solo se actualizaron; ajustado `update_databases.sh` para que la anotación no rompa la extracción de nombres de módulo.
- **Sexto hallazgo, real y ya corregido**: probando `update_databases.sh` en la rama `19.0` sobre dos bases (`maralva19`, `proasur19`), `proasur19` falló con `ERROR: operator does not exist: character varying -> unknown` al actualizar `res_users` — desajuste entre el esquema de esa BD y lo que el código actual espera. Causa: la lista de módulos para el `-u` viene del fichero generado por `update_oca_upstream.sh`, que excluye a propósito el core (OCB); si el core cambió algo que necesita migración de esquema, un `-u` limitado a módulos OCA nunca lo aplica. `maralva19`, creada más tarde (con el esquema ya al día), no tuvo el problema. **Decisión**: `update_databases.sh` pasa a usar siempre `-u all` en vez de la lista de módulos concretos — el fichero de módulos sigue mostrándose, pero solo como referencia de qué probar después. El usuario confirmó que estas actualizaciones son periódicas (mensuales o menos), así que el coste extra de tiempo de `-u all` no es un problema real.
- **Séptimo hallazgo, específico de correr en la rama `19.0`**: incluso con `-u all`, `proasur19` volvió a fallar — esta vez con `psycopg2.errors.InvalidTextRepresentation: invalid input syntax for type json` al actualizar el módulo `base` ("Token 'base' is invalid"). Es Odoo intentando convertir una columna existente (datos antiguos en texto plano) al nuevo formato jsonb, y fallando porque el valor guardado no es JSON válido — un problema de migración de esquema del propio core, no de nuestros scripts, más probable por lo activa/inestable que está la rama `19.0` en desarrollo. Como `proasur19` era una base de pruebas vacía, se resolvió con `dropdb` + recrearla desde cero contra el código ya actualizado, en vez de perseguir la migración fina. `update_databases.sh` queda validado igualmente; este tipo de fallo de migración de esquema en `19.0` puede repetirse mientras la rama esté en desarrollo activo, y no siempre habrá una base vacía que se pueda simplemente recrear (ahí sí haría falta un script de migración de `openupgradelib` de verdad).
- **Octavo hallazgo, real y ya corregido**: `push_org_origin.sh` falló al probarlo con la rama `18.0` — `git push` normal rechazado como "non-fast-forward" al subir `OCB`. Causa: `update_oca_upstream.sh` había avisado antes de 1 commit divergente en ese repo (el fork tenía algo que OCA no tiene) y aun así hizo `reset --hard upstream/18.0`, descartándolo localmente — pero el fork en GitHub seguía teniéndolo, así que un push normal (fast-forward) no puede aplicarse. **Decisión, confirmada con el usuario por ser una operación que reescribe historial**: `push_org_origin.sh` pasa a usar `git push --force-with-lease` en vez de un push normal — es la continuación lógica de que `update_oca_upstream.sh` ya descarta divergencias con el visto bueno del usuario; `--force-with-lease` (no `--force` a secas) aborta si alguien más ha tocado el fork entre medias.
- **Petición del usuario — bloqueo de integridad de los repos OCA**: añadido a `push_org_origin.sh` un chequeo antes de subir cada repo (`git rev-list upstream/$BRANCH..HEAD --count`): si hay commits locales que OCA no tiene, no se sube ese repo (los demás siguen), avisando claramente — motivo: evitar que se cuele a GitHub código editado a mano directamente dentro de `oca/<repo>` (los repos de OCA deben ser espejos limpios; los cambios propios van en `maralva-custom`). Verificado con una simulación (repo limpio vs. repo con un commit manual de más) antes de subir.

**Pendiente para la próxima sesión:**
- Diseñar y crear los scripts de instalación única por versión (Pendientes #1-3), que ya deben referenciar los certificados de `maralva-ops/certs/` y replicar el patrón de `04-enable-ssl-multi.sh`.
- Ejecutar `certs/setup-ssl-duckdns.sh` y luego `scripts/04-enable-ssl-multi.sh` en un servidor real por primera vez, para `maralva18.maralva.eu` (ver `maralva-ops/ESTADO.md`).
- Instalar/autenticar `gh` en el servidor para que el fork automático funcione también con la rama `19.0`, y revisar qué repos de OCA quedan omitidos.
- Probar de extremo a extremo el nuevo flujo de actualización desde OCA (Pendientes #9).
- Lanzar la instalación real de la rama `19.0` en la otra máquina (esta prueba de `19.0` fue en la misma VM que `18.0`, solo para validar el comportamiento multi-instancia).

**Notas:**
- El dato de que SupremeDNS también sirve el correo del dominio fue clave para descartar una migración completa de zona DNS y quedarse con la delegación puntual de `_acme-challenge.maralva.eu`.
- La limitación de DuckDNS con el router del operador (no soportado como cliente DDNS) resultó irrelevante para el reto ACME, que no usa el router en absoluto.
- El usuario está probando primero en una VM clonada (misma IP que la original, nunca las dos arrancadas a la vez) antes de crear las ramas `18.0`/`19.0` de instalación única; la 19.0 se lanzará después en la otra máquina.
